name: Open Pull Request

# A caller, not a copy: every job body comes from the pinned pipeline commit, so adopting
# an update is a one-line change here and the diff shows exactly what moved.
on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Head branch name (e.g. feature/my-feature)'
        required: true
        type: string
      title:
        description: 'Pull Request title'
        required: true
        type: string
      body:
        description: 'Pull Request description'
        required: true
        type: string
      base:
        description: 'Target base branch'
        required: false
        type: string
        default: 'main'
      draft:
        description: 'Create as draft PR'
        required: false
        type: boolean
        default: true

permissions:
  contents: write
  pull-requests: write
  actions: write

jobs:
  open-pr:
    uses: marius-patrik/DarkFactory/.github/workflows/open-pr.yml@757c89a91f48560e32bbb3315d5b1a1bde8f3cf9
    with:
      branch: ${{ inputs.branch }}
      title: ${{ inputs.title }}
      body: ${{ inputs.body }}
      base: ${{ inputs.base }}
      draft: ${{ inputs.draft }}
      pipeline-repo: marius-patrik/DarkFactory
      pipeline-ref: "757c89a91f48560e32bbb3315d5b1a1bde8f3cf9"
    secrets: inherit

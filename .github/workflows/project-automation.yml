name: Project Board Automation

on:
  issues:
    types: [opened, labeled, unlabeled, closed, reopened]
  pull_request:
    types: [opened, edited, closed, ready_for_review, reopened]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write
  repository-projects: write

jobs:
  project-automation:
    uses: marius-patrik/DarkFactory/.github/workflows/project-automation.yml@darkfactory
    with:
      pipeline-repo: marius-patrik/DarkFactory
      pipeline-ref: "52816adc67cf9327831ecba775e8c9fdde764051"
    secrets: inherit
